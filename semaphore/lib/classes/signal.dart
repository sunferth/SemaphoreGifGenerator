import 'package:semaphore/enum/hand_position.dart';
import 'package:semaphore/enum/signal_type.dart';

class Signal {
  ///The type of signal it is
  SignalType type;


  Signal({required this.type});
}